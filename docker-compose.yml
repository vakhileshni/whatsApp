version: '3.8'

services:
  whatsapp-app:
    build: .
    container_name: whatsapp-business-app
    ports:
      - "4000:4000"   # Backend API
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      # Optional: Mount for development (uncomment if needed)
      # - ./backend:/app/backend
      # - ./frontend:/app/frontend
    restart: unless-stopped


